import chalk from 'chalk';

class Service {
  private cwd: string;
  private plugins: any[];

  constructor(cwd) {
    this.cwd = cwd || process.cwd();
  }

  initPlugins = () => {

  };

  resolvePlugins = () => {

  }
}
